import type { FastifyReply, FastifyRequest } from "fastify";
import type {ChannelCreateSchema, createChannelInput} from "./channel.schema.js"

export async function createChannel(createChannelInput: createChannelInput){
    
}